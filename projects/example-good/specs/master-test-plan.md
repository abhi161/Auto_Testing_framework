# Master Test Plan — example-good

> **Generated by:** Test Planner
> **Run scope:** Functional / E2E only
> **Target:** `https://example.com`
> **Design source of truth:** Attached PNG (1 image) — tokens verified in `specs/project-memory.md` §5c
> **Authentication:** None required (public page)
> **Synced with:** `docs/actualtestcases.csv` (TC-001 – TC-006)

---

## Traceability matrix

| TC No. | Summary | Business Rule | Source | Spec file | Status |
|--------|---------|---------------|--------|-----------|--------|
| TC-001 | Page loads — HTTP 200 + title contains "Example Domain" | BR-001 | Static HTML `/` — page-level | `tests/homepage.spec.ts` | ✅ PASS |
| TC-002 | H1 heading exact text "Example Domain" | BR-001 | Static HTML `<h1>` element | `tests/homepage.spec.ts` | ✅ PASS |
| TC-003 | Paragraph contains documentation-examples sentence | BR-002 | Static HTML `<p>` element (first) | `tests/homepage.spec.ts` | ✅ PASS |
| TC-004 | Paragraph contains "Avoid use in operations." | BR-003 | Static HTML `<p>` element (first) | `tests/homepage.spec.ts` | ✅ PASS |
| TC-005 | "Learn more" link is visible | BR-004 | Static HTML `<a>` element | `tests/homepage.spec.ts` | ✅ PASS |
| TC-006 | "Learn more" link href is a valid absolute URL | BR-004 | Static HTML `<a href>` attribute | `tests/homepage.spec.ts` | ✅ PASS |

---

## Detailed test case entries

### TC-001: Page loads successfully (HTTP 200 + title)
- **Test Type:** Functional / E2E
- **Source:** `https://example.com` — HTTP response + `<title>` tag
- **Business Rule:** BR-001 — Heading MUST read "Example Domain"; page title MUST contain "Example Domain"
- **Design source:** Attached PNG — heading reads "Example Domain" confirming page identity
- **Steps:**
  1. Navigate to `TEST_URL` (`https://example.com`)
  2. Await `networkidle`
  3. Capture `response.status()` and `page.title()`
  4. Assert `response.status() === 200`
  5. Assert `page.title()` matches `/Example Domain/`
- **Expected outcome:** HTTP 200 OK; page title contains `'Example Domain'`
- **Selector:** N/A (response object + `page.title()`)
- **Spec file:** `tests/homepage.spec.ts`

---

### TC-002: H1 heading exact text match
- **Test Type:** Functional / E2E
- **Source:** Static HTML `<h1>` — single heading element on page
- **Business Rule:** BR-001 — Heading MUST read exactly `"Example Domain"`
- **Design source:** Attached PNG — `<h1>` clearly reads `"Example Domain"` in bold charcoal
- **Steps:**
  1. Navigate to `TEST_URL`
  2. Locate selector `h1`
  3. Read `textContent()` and `.trim()`
  4. Assert strict equality to `'Example Domain'`
- **Expected outcome:** `h1.textContent().trim() === 'Example Domain'`
- **Selector:** `h1` (tier 3 — single stable tag; confirmed ✅)
- **Spec file:** `tests/homepage.spec.ts`

---

### TC-003: Paragraph first sentence present
- **Test Type:** Functional / E2E
- **Source:** Static HTML `<p>` — first paragraph element
- **Business Rule:** BR-002 — Paragraph MUST contain the documentation-examples sentence
- **Design source:** Attached PNG — paragraph line 1: `"This domain is for use in documentation examples without needing permission."`
- **Steps:**
  1. Navigate to `TEST_URL`
  2. Locate `page.locator('p').first()`
  3. Read `textContent()`
  4. Assert includes `'This domain is for use in documentation examples without needing permission.'`
- **Expected outcome:** Paragraph text includes the full first sentence
- **Selector:** `page.locator('p').first()` (tier 3 — confirmed ✅)
- **Spec file:** `tests/homepage.spec.ts`

---

### TC-004: Paragraph second sentence present
- **Test Type:** Functional / E2E
- **Source:** Static HTML `<p>` — first paragraph element
- **Business Rule:** BR-003 — Paragraph MUST contain `"Avoid use in operations."`
- **Design source:** Attached PNG — paragraph line 2 reads `"Avoid use in operations."`
- **Steps:**
  1. Navigate to `TEST_URL`
  2. Locate `page.locator('p').first()`
  3. Read `textContent()`
  4. Assert includes `'Avoid use in operations.'`
- **Expected outcome:** Paragraph text includes `'Avoid use in operations.'`
- **Selector:** `page.locator('p').first()` (tier 3 — confirmed ✅)
- **Spec file:** `tests/homepage.spec.ts`

---

### TC-005: "Learn more" link is visible
- **Test Type:** Functional / E2E
- **Source:** Static HTML `<a>` element below the paragraph
- **Business Rule:** BR-004 — A "Learn more" hyperlink MUST be present, visible, and have a valid `href`
- **Design source:** Attached PNG — muted blue/indigo underlined link labelled `"Learn more"` visible below paragraph
- **Steps:**
  1. Navigate to `TEST_URL`
  2. `page.getByRole('link', { name: 'Learn more' })`
  3. Assert `isVisible() === true`
- **Expected outcome:** Link `'Learn more'` is rendered and visible in the viewport
- **Selector:** `page.getByRole('link', { name: 'Learn more' })` (tier 2 — ARIA role + name; confirmed ✅)
- **Spec file:** `tests/homepage.spec.ts`

---

### TC-006: "Learn more" link href is a valid absolute URL
- **Test Type:** Functional / E2E
- **Source:** Static HTML `<a href="...">` attribute value
- **Business Rule:** BR-004 — href MUST be a non-empty absolute URL
- **Design source:** Attached PNG — link is present and styled as a navigable hyperlink
- **Steps:**
  1. Navigate to `TEST_URL`
  2. `page.getByRole('link', { name: 'Learn more' })`
  3. `getAttribute('href')`
  4. Assert value is non-empty and matches `^https?://`
- **Expected outcome:** `href` is a non-empty string matching `^https?://`
- **Selector:** `page.getByRole('link', { name: 'Learn more' })` (tier 2 — confirmed ✅)
- **Spec file:** `tests/homepage.spec.ts`

---

## Deferred test cases (out of scope this run)

| TC | Summary | Test Type | Reason deferred |
|----|---------|-----------|-----------------|
| TC-VIS-001 | Background colour matches design `#f0f0f2` | Visual / Design Parity | Not requested this run |
| TC-VIS-002 | H1 font-weight is bold (~700) | Visual / Design Parity | Not requested this run |
| TC-VIS-003 | Link colour is muted blue/indigo | Visual / Design Parity | Not requested this run |

---

## Business Rules coverage

| BR | Description | Covered by |
|----|-------------|------------|
| BR-001 | H1 = "Example Domain"; title contains "Example Domain" | TC-001, TC-002 |
| BR-002 | Paragraph contains documentation-examples sentence | TC-003 |
| BR-003 | Paragraph contains "Avoid use in operations." | TC-004 |
| BR-004 | "Learn more" link is visible with valid absolute href | TC-005, TC-006 |
| BR-005 | Background ~`#f0f0f2` | Deferred (Visual / Design Parity) |

**Coverage:** 4 / 4 in-scope business rules covered (100%). BR-005 deferred by scope filter.

---

## Selector coverage summary

| Selector | Tier | Elements covered | Confirmed |
|----------|------|------------------|-----------|
| `h1` | 3 | TC-002 | ✅ |
| `page.locator('p').first()` | 3 | TC-003, TC-004 | ✅ |
| `page.getByRole('link', { name: 'Learn more' })` | 2 | TC-005, TC-006 | ✅ |

> No `data-testid` or `aria-label` attributes present on `https://example.com` (static HTML, no framework). Tier 1 unavailable — tier 2/3 are the highest available and are stable.

---

## Handoff to Test Generator / Executor ✅

- `docs/actualtestcases.csv` — **6 rows confirmed, in sync with this plan**
- `specs/selectors.md` — confirmed, all selectors tier 2–3, all validated
- `tests/homepage.spec.ts` — already generated and previously PASSED (all 6 TCs)
- **Action:** Run `npx playwright test homepage.spec.ts` under `projects/example-good/` and produce `specs/test-execution-report.md`
