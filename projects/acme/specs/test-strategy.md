# Test Strategy — acme
_Generated by: Context Analyst_
_Last updated: 2026-07-13_

---

## §0. Requested Test Types (scope filter)
Requested: **Functional / E2E**, **UI Testing**
(Functional/E2E is always in scope; UI Testing = design-parity assertions against attached design images.)

---

## §1. Environment

| Variable | Value |
|---|---|
| `TEST_URL` | `https://example.com/` |
| `TEST_AUTH_URL` | _(none — public site)_ |
| `TEST_USER` | _(none — no auth)_ |
| `TEST_PASS` | _(none — no auth)_ |
| `IDENTITY_PROVIDER` | _(none — no auth)_ |

---

## §2. Authentication

- **Type:** None — `example.com` is a fully public static page.
- No login flow, no OAuth URL, no auth helper invocation required.
- No `storageState` needed.

---

## §3. Application Architecture

**Source:** Static HTML (IANA-hosted infrastructure). No client-side framework detected.

| Route | Screen | Auth Required? | Notes |
|---|---|---|---|
| `https://example.com/` | Home | No | Single static HTML page |
| _(external — resolved at runtime)_ | Screen 2 (IANA reserved domains) | No | Destination of "Learn more" link; assert by content, not URL |

**Key elements (from design):**
- Home: `<h1>`, `<p>`, `<a>` ("Learn more") — no header, nav, logo, or footer
- Screen 2: IANA header with logo + nav (`Domains`, `Protocols`, `Numbers`, `About`), `<h1>` = "Example Domains", body with RFC links, "Further Reading" section, footer

---

## §4. Design Source of Truth

| Field | Value |
|---|---|
| Source type | Attached design images (model-read directly) |
| Figma URL/Key | None |
| Access method | Model vision — images provided in task |
| Screen 1 image | `docs/design/Screenshot_from_2026-07-13_12-14-01.png` |
| Screen 2 image | `docs/design/Screenshot_from_2026-07-13_12-14-19.png` |

**Design-spec summary:** See `specs/design-spec.md` for full token extraction.

Critical design expectations:
- **Home:** `<h1>` = "Example Domain", body text exact match, blue "Learn more" link, grey `#eee` background, **no nav / header / footer / logo**
- **Screen 2 (IANA):** `<h1>` = "Example Domains", 4-item top nav (Domains, Protocols, Numbers, About), RFC 2606 + RFC 6761 referenced, "Further Reading" section, "IANA-managed Reserved Domains" bullet, footer with "Privacy Policy" + "Terms of Service", "Last revised 2017-05-13."

---

## §5. Business Rules

| ID | Rule |
|---|---|
| BR-001 | Home `<h1>` MUST be exactly `Example Domain` |
| BR-002 | Home body paragraph MUST be exactly `This domain is for use in documentation examples without needing permission. Avoid use in operations.` |
| BR-003 | A "Learn more" link MUST be present on the home page |
| BR-004 | Home page MUST NOT contain any header, navigation bar, logo, or footer |
| BR-005 | Clicking "Learn more" MUST navigate to a page whose `<h1>` is `Example Domains` |
| BR-006 | Screen 2 top-nav MUST contain exactly 4 items: Domains, Protocols, Numbers, About (in that order) |
| BR-007 | Screen 2 MUST show a "Further Reading" section with an "IANA-managed Reserved Domains" bullet link |
| BR-008 | Screen 2 footer MUST contain "Privacy Policy" and "Terms of Service" links |
| BR-009 | Screen 2 MUST reference RFC 2606 and RFC 6761 (as visible links in the body) |
| BR-010 | Screen 2 MUST show "Last revised 2017-05-13." |

---

## §6. Test Cases

### User-provided test cases
No pre-existing rows in `docs/actualtestcases.csv` (empty headers only).

### Discovered + design-parity test cases

| TC # | Summary | Type | BR | Steps | Expected Outcome |
|---|---|---|---|---|---|
| TC-001 | Home — page loads and `<h1>` matches design | Functional / UI | BR-001 | Open `TEST_URL`; assert `h1` text | `Example Domain` |
| TC-002 | Home — body paragraph text matches design | Functional / UI | BR-002 | Open `TEST_URL`; assert `p` text | `This domain is for use in documentation examples without needing permission. Avoid use in operations.` |
| TC-003 | Home — "Learn more" link is present | Functional | BR-003 | Open `TEST_URL`; assert link visible | Link with text "Learn more" is visible |
| TC-004 | Home — NO header/nav/logo/footer present | UI | BR-004 | Open `TEST_URL`; assert absence of `header`, `nav`, `footer`, logo | None of those elements exist |
| TC-005 | Home — background colour matches design (`#eeeeee`) | UI | BR-001 | Open `TEST_URL`; check `body` computed `background-color` | ≈ `rgb(238, 238, 238)` |
| TC-006 | Navigate via "Learn more" → Screen 2 loads | Functional / E2E | BR-005 | Click "Learn more"; wait for load | Page `<h1>` = `Example Domains` |
| TC-007 | Screen 2 — `<h1>` matches design | UI | BR-005 | After navigation; assert `h1` | `Example Domains` |
| TC-008 | Screen 2 — top-nav has exactly 4 items (Domains, Protocols, Numbers, About) | UI | BR-006 | Assert nav items count and text | 4 items, correct labels in order |
| TC-009 | Screen 2 — "Further Reading" section present | UI | BR-007 | Assert heading text `Further Reading` visible | Heading present |
| TC-010 | Screen 2 — "IANA-managed Reserved Domains" bullet link present | UI | BR-007 | Assert link text visible | Link present and visible |
| TC-011 | Screen 2 — footer "Privacy Policy" link present | UI | BR-008 | Assert footer link text | Present |
| TC-012 | Screen 2 — footer "Terms of Service" link present | UI | BR-008 | Assert footer link text | Present |
| TC-013 | Screen 2 — RFC 2606 link present in body | UI | BR-009 | Assert `a:has-text("RFC 2606")` | Present |
| TC-014 | Screen 2 — RFC 6761 link present in body | UI | BR-009 | Assert `a:has-text("RFC 6761")` | Present |
| TC-015 | Screen 2 — "Last revised 2017-05-13." footer text present | UI | BR-010 | Assert text in page | Present |

---

## §7. Handoff notes for Test Planner

1. Write `docs/actualtestcases.csv` rows for TC-001 → TC-015.
2. Confirm selectors via live UI exploration (especially the "Learn more" link — could be `a:has-text("More information")` or `a:has-text("Learn more")`; assert at runtime).
3. Check whether "Learn more" opens same tab or new tab — handle accordingly (`page.waitForNavigation` vs `context.waitForEvent('page')`).
4. The IANA page destination URL is unverified — do NOT hard-code; assert by `h1` content only.
5. `docs/design/` images are the visual baselines — the Generator may use `toHaveScreenshot` with appropriate masking for dynamic regions.
